import RevealOnScroll from './RevealOnScroll';

/**
 * MotionReveal alias pointing to lightweight RevealOnScroll.
 * This ensures legacy imports don't pull in framer-motion.
 */
export default RevealOnScroll;
